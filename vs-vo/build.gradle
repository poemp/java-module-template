plugins {
    id 'java'
}

group 'org.poem'
version '1.0-SNAPSHOT'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    compile( project(":vs-api") )
    compile( project(":vs-model") )
    compile( project(":vs-common") )
    compile( project(":vs-dao") )
}
